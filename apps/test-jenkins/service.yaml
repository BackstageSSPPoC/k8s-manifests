apiVersion: v1
kind: Service
metadata:
  name: test-jenkins
spec:
  selector:
    app: test-jenkins
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: test-jenkins-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: test-jenkins.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: test-jenkins
#             port:
#               number: 80
