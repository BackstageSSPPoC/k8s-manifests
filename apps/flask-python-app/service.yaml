apiVersion: v1
kind: Service
metadata:
  name: flask-python-app
spec:
  selector:
    app: flask-python-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: flask-python-app-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: flask-python-app.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: flask-python-app
#             port:
#               number: 80
