apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: flask-python-app
  namespace: argocd
  labels:
    app.kubernetes.io/name: flask-python-app
spec:
  project: default
  source:
    repoURL: https://github.com/BackstageSSPPoC/k8s-manifests.git
    targetRevision: main
    path: apps/flask-python-app
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
