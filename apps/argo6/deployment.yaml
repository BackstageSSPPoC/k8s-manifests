apiVersion: apps/v1
kind: Deployment
metadata:
  name: argo6
  labels:
    backstage.io/kubernetes-id: argo6
spec:
  replicas: 1
  selector:
    matchLabels:
      app: argo6
  template:
    metadata:
      labels:
        app: argo6
        backstage.io/kubernetes-id: argo6
    spec:
      containers:
        - name: argo6
          image: chaitanyapandeygspann/argo6:1.0.1
          imagePullPolicy: Always
          # ports:
          #   - containerPort: 3000