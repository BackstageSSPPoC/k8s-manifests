apiVersion: v1
kind: Service
metadata:
  name: demo-27apr-sonar-vault-testing
spec:
  selector:
    app: demo-27apr-sonar-vault-testing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: demo-27apr-sonar-vault-testing-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: demo-27apr-sonar-vault-testing.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: demo-27apr-sonar-vault-testing
#             port:
#               number: 80
