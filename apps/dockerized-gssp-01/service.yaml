apiVersion: v1
kind: Service
metadata:
  name: dockerized-gssp-01
spec:
  selector:
    app: dockerized-gssp-01
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dockerized-gssp-01-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dockerized-gssp-01.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dockerized-gssp-01
#             port:
#               number: 80
