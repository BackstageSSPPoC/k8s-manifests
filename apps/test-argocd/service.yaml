apiVersion: v1
kind: Service
metadata:
  name: test-argocd
spec:
  selector:
    app: test-argocd
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: test-argocd-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: test-argocd.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: test-argocd
#             port:
#               number: 80
