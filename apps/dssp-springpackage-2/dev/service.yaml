apiVersion: v1
kind: Service
metadata:
  name: dssp-springpackage-2
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-springpackage-2
spec:
  selector:
    app: dssp-springpackage-2
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
