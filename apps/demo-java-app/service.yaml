apiVersion: v1
kind: Service
metadata:
  name: demo-java-app
spec:
  selector:
    app: demo-java-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: demo-java-app-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: demo-java-app.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: demo-java-app
#             port:
#               number: 80