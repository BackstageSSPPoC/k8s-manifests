apiVersion: v1
kind: Service
metadata:
  name: python-project
  namespace: dev
  labels:
    backstage.io/kubernetes-id: python-project
    environment: dev
spec:
  selector:
    app: python-project
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP