apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-5
  namespace: prod
  labels:
    backstage.io/kubernetes-id: dssp-targetp-5
    environment: prod
spec:
  selector:
    app: dssp-targetp-5
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
 


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dssp-targetp-5-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dssp-targetp-5.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dssp-targetp-5
#             port:
#               number: 80
