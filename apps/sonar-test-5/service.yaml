apiVersion: v1
kind: Service
metadata:
  name: sonar-test-5
spec:
  selector:
    app: sonar-test-5
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: sonar-test-5-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: sonar-test-5.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: sonar-test-5
#             port:
#               number: 80
