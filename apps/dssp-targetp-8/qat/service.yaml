apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-8
  namespace: qat
  labels:
    backstage.io/kubernetes-id: dssp-targetp-8
    environment: qat
spec:
  selector:
    app: dssp-targetp-8
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
 


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dssp-targetp-8-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dssp-targetp-8.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dssp-targetp-8
#             port:
#               number: 80
