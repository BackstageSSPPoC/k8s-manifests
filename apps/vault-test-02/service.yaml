apiVersion: v1
kind: Service
metadata:
  name: vault-test-02
spec:
  selector:
    app: vault-test-02
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: vault-test-02-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: vault-test-02.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: vault-test-02
#             port:
#               number: 80