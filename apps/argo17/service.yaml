# apiVersion: v1
# kind: Service
# metadata:
#   name: argo17
# spec:
#   selector:
#     app: argo17
#   ports:
#     - protocol: TCP
#       port: 80
#       targetPort: 8080
#   type: ClusterIP


apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argo17-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
    networking.gke.io/managed-certificates: "app-cert"
spec:
  rules:
  - host: argo17.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argo17
            port:
              number: 80