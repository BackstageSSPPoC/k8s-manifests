apiVersion: v1
kind: Service
metadata:
  name: dssp-springboot-3
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-springboot-3
spec:
  selector:
    app: dssp-springboot-3
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
