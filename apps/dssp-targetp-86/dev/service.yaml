apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-86
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-86
spec:
  selector:
    app: dssp-targetp-86
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
