apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-targetp-36
  namespace: dev
  labels:
    app: dssp-targetp-36
    backstage.io/kubernetes-id: dssp-targetp-36
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-targetp-36
  template:
    metadata:
      labels:
        app: dssp-targetp-36
        backstage.io/kubernetes-id: dssp-targetp-36
        environment: dev
    spec:
      containers:
        - name: dssp-targetp-36
          image: chaitanyapandeygspann/dssp-targetp-36:1.0.1
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"