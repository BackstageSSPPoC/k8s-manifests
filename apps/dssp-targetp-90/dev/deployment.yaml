apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-targetp-90
  namespace: dev
  labels:
    app: dssp-targetp-90
    backstage.io/kubernetes-id: dssp-targetp-90
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-targetp-90
  template:
    metadata:
      labels:
        app: dssp-targetp-90
        backstage.io/kubernetes-id: dssp-targetp-90
        environment: dev
    spec:
      containers:
        - name: dssp-targetp-90
          image: chaitanyapandeygspann/dssp-targetp-90:1.0.1
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"
            - name: URL_PREFIX
              value: /dssp-targetp-90-dev
          resources:
            requests:
              memory: "384Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
