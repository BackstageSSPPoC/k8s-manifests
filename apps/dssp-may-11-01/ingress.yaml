apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-may-11-01
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /dssp-may-11-01
            pathType: Prefix
            backend:
              service:
                name: dssp-may-11-01
                port:
                  number: 80
