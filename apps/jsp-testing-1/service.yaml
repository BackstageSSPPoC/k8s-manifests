apiVersion: v1
kind: Service
metadata:
  name: jsp-testing-1
spec:
  selector:
    app: jsp-testing-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: ${APP_PORTS}
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: jsp-testing-1-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: jsp-testing-1.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: jsp-testing-1
#             port:
#               number: 80