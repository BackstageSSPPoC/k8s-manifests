apiVersion: v1
kind: Service
metadata:
  name: dssp-nodejs-project
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-nodejs-project
spec:
  selector:
    app: dssp-nodejs-project
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
