apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-targetp-81
  namespace: dev
  labels:
    app: dssp-targetp-81
    backstage.io/kubernetes-id: dssp-targetp-81
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-targetp-81
  template:
    metadata:
      labels:
        app: dssp-targetp-81
        backstage.io/kubernetes-id: dssp-targetp-81
        environment: dev
    spec:
      containers:
        - name: dssp-targetp-81
          image: chaitanyapandeygspann/dssp-targetp-81:1.0.1
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"
          resources:
            requests:
              memory: "384Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
