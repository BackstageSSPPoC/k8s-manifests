apiVersion: v1
kind: Service
metadata:
  name: gssp-march
spec:
  selector:
    app: gssp-march
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: gssp-march-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: gssp-march.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: gssp-march
#             port:
#               number: 80
