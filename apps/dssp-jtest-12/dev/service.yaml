apiVersion: v1
kind: Service
metadata:
  name: dssp-jtest-12
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-jtest-12
spec:
  selector:
    app: dssp-jtest-12
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
