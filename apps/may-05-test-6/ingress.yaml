apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: may-05-test-6-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: may-05-test-6
                port:
                  number: 80
