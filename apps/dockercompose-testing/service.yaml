apiVersion: v1
kind: Service
metadata:
  name: dockercompose-testing
spec:
  selector:
    app: dockercompose-testing
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dockercompose-testing-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dockercompose-testing.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dockercompose-testing
#             port:
#               number: 80
