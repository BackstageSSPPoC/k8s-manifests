apiVersion: apps/v1
kind: Deployment
metadata:
  name: multilang-python
  namespace: undefined
  labels:
    app: multilang-python
    backstage.io/kubernetes-id: multilang-python
    environment: undefined
spec:
  replicas: 1
  selector:
    matchLabels:
      app: multilang-python
  template:
    metadata:
      labels:
        app: multilang-python
        backstage.io/kubernetes-id: multilang-python
        environment: undefined
    spec:
      containers:
        - name: multilang-python
          image: chaitanyapandeygspann/multilang-python:1.0.7
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "undefined"
