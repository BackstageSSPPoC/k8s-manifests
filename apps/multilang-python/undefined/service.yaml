apiVersion: v1
kind: Service
metadata:
  name: multilang-python
  namespace: undefined
  labels:
    backstage.io/kubernetes-id: multilang-python
spec:
  selector:
    app: multilang-python
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
