apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-91
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-91
spec:
  selector:
    app: dssp-targetp-91
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
