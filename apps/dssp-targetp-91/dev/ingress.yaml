apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-targetp-91
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-91
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dssp-targetp-91-dev
            pathType: Prefix
            backend:
              service:
                name: dssp-targetp-91
                port:
                  number: 80
