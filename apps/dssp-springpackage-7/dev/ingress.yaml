apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-springpackage-7
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-springpackage-7
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dssp-springpackage-7-dev
            pathType: Prefix
            backend:
              service:
                name: dssp-springpackage-7
                port:
                  number: 80
