apiVersion: v1
kind: Service
metadata:
  name: dssp-sonar-check
spec:
  selector:
    app: dssp-sonar-check
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dssp-sonar-check-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dssp-sonar-check.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dssp-sonar-check
#             port:
#               number: 80
