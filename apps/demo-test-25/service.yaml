apiVersion: v1
kind: Service
metadata:
  name: demo-test-25
spec:
  selector:
    app: demo-test-25
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: demo-test-25-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: demo-test-25.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: demo-test-25
#             port:
#               number: 80
