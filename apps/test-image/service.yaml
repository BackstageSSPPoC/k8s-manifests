apiVersion: v1
kind: Service
metadata:
  name: test-image
spec:
  selector:
    app: test-image
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: test-image-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: test-image.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: test-image
#             port:
#               number: 80
