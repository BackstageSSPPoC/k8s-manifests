apiVersion: v1
kind: Service
metadata:
  name: test-kube-1
spec:
  selector:
    app: test-kube-1
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: test-kube-1-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: test-kube-1.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: test-kube-1
#             port:
#               number: 80
