apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-springboot-project
  namespace: dev
  labels:
    app: dssp-springboot-project
    backstage.io/kubernetes-id: dssp-springboot-project
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-springboot-project
  template:
    metadata:
      labels:
        app: dssp-springboot-project
        backstage.io/kubernetes-id: dssp-springboot-project
        environment: dev
    spec:
      containers:
        - name: dssp-springboot-project
          image: chaitanyapandeygspann/dssp-springboot-project:1.0.1
          ports:
            - containerPort: 8080
          env:
            - name: APP_ENV
              value: "dev"
            - name: URL_PREFIX
              value: /dssp-springboot-project-dev
          resources:
            requests:
              memory: "384Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
