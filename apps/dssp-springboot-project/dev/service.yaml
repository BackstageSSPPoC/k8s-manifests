apiVersion: v1
kind: Service
metadata:
  name: dssp-springboot-project
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-springboot-project
spec:
  selector:
    app: dssp-springboot-project
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
