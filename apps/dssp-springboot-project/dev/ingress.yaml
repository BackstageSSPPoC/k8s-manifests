apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-springboot-project
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-springboot-project
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dssp-springboot-project-dev
            pathType: Prefix
            backend:
              service:
                name: dssp-springboot-project
                port:
                  number: 80
