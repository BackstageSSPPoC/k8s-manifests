apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-12
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-12
    environment: dev
spec:
  selector:
    app: dssp-targetp-12
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP