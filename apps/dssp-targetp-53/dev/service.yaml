apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-53
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-53
spec:
  selector:
    app: dssp-targetp-53
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
