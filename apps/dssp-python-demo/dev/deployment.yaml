apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-python-demo
  namespace: dev
  labels:
    app: dssp-python-demo
    backstage.io/kubernetes-id: dssp-python-demo
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-python-demo
  template:
    metadata:
      labels:
        app: dssp-python-demo
        backstage.io/kubernetes-id: dssp-python-demo
        environment: dev
    spec:
      containers:
        - name: dssp-python-demo
          image: chaitanyapandeygspann/dssp-python-demo:1.0.3
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"
            - name: URL_PREFIX
              value: /dssp-python-demo-dev
          resources:
            requests:
              memory: "384Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
