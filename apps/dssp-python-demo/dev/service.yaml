apiVersion: v1
kind: Service
metadata:
  name: dssp-python-demo
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-python-demo
spec:
  selector:
    app: dssp-python-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
