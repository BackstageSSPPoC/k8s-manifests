apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-48
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-48
spec:
  selector:
    app: dssp-targetp-48
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
