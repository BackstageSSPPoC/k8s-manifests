apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-targetp-41
  namespace: dev
  labels:
    app: dssp-targetp-41
    backstage.io/kubernetes-id: dssp-targetp-41
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-targetp-41
  template:
    metadata:
      labels:
        app: dssp-targetp-41
        backstage.io/kubernetes-id: dssp-targetp-41
        environment: dev
    spec:
      containers:
        - name: dssp-targetp-41
          image: chaitanyapandeygspann/dssp-targetp-41:1.0.3
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"
