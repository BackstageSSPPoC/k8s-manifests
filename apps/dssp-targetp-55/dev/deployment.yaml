apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-targetp-55
  namespace: dev
  labels:
    app: dssp-targetp-55
    backstage.io/kubernetes-id: dssp-targetp-55
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-targetp-55
  template:
    metadata:
      labels:
        app: dssp-targetp-55
        backstage.io/kubernetes-id: dssp-targetp-55
        environment: dev
    spec:
      containers:
        - name: dssp-targetp-55
          image: chaitanyapandeygspann/dssp-targetp-55:1.0.1
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"
