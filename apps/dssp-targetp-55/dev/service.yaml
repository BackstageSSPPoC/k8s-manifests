apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-55
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-55
spec:
  selector:
    app: dssp-targetp-55
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
