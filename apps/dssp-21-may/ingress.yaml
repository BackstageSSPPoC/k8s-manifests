apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-21-may
  namespace: default
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - path: /dssp-21-may
            pathType: Prefix
            backend:
              service:
                name: dssp-21-may
                port:
                  number: 80
