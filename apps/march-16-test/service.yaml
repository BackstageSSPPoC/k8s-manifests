apiVersion: v1
kind: Service
metadata:
  name: march-16-test
spec:
  selector:
    app: march-16-test
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: march-16-test-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: march-16-test.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: march-16-test
#             port:
#               number: 80
