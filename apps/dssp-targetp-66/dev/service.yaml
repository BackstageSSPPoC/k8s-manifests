apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-66
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-66
spec:
  selector:
    app: dssp-targetp-66
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
