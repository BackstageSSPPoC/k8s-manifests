apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-54
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-54
spec:
  selector:
    app: dssp-targetp-54
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
