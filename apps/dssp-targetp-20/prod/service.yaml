apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-20
  namespace: prod
  labels:
    backstage.io/kubernetes-id: dssp-targetp-20
    environment: prod
spec:
  selector:
    app: dssp-targetp-20
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
 


# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: dssp-targetp-20-ingress
# spec:
#   ingressClassName: gce
#   rules:
#   - host: dssp-targetp-20.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: dssp-targetp-20
#             port:
#               number: 80
