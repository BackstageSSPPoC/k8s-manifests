apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-52
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-52
spec:
  selector:
    app: dssp-targetp-52
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
