apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-82
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-82
spec:
  selector:
    app: dssp-targetp-82
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
