apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-targetp-82
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-82
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dssp-targetp-82-dev
            pathType: Prefix
            backend:
              service:
                name: dssp-targetp-82
                port:
                  number: 80
