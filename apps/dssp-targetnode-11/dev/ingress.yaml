apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dssp-targetnode-11
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetnode-11
    environment: dev
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: /dssp-targetnode-11-dev
            pathType: Prefix
            backend:
              service:
                name: dssp-targetnode-11
                port:
                  number: 80
