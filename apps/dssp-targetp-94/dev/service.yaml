apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-94
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-94
spec:
  selector:
    app: dssp-targetp-94
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
