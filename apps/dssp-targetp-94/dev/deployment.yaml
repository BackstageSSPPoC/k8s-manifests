apiVersion: apps/v1
kind: Deployment
metadata:
  name: dssp-targetp-94
  namespace: dev
  labels:
    app: dssp-targetp-94
    backstage.io/kubernetes-id: dssp-targetp-94
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dssp-targetp-94
  template:
    metadata:
      labels:
        app: dssp-targetp-94
        backstage.io/kubernetes-id: dssp-targetp-94
        environment: dev
    spec:
      containers:
        - name: dssp-targetp-94
          image: chaitanyapandeygspann/dssp-targetp-94:1.0.3
          ports:
            - containerPort: 5000
          env:
            - name: APP_ENV
              value: "dev"
            - name: URL_PREFIX
              value: /dssp-targetp-94-dev
          resources:
            requests:
              memory: "384Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
