apiVersion: v1
kind: Service
metadata:
  name: dssp-targetp-42
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-targetp-42
spec:
  selector:
    app: dssp-targetp-42
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: ClusterIP
