apiVersion: v1
kind: Service
metadata:
  name: dssp-springpackage-6
  namespace: dev
  labels:
    backstage.io/kubernetes-id: dssp-springpackage-6
spec:
  selector:
    app: dssp-springpackage-6
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
